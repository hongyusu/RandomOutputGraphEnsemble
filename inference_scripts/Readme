


1. This file
 Readme

2. Run base learner (MMCRF) on 'one dataset' with 'one output graph'.
    example run 'run_baselearner('fpuni','tree')' will run base learner on 'fpuni' dataset with 'tree' as random output grap structure.
    files included: 
        script to run baselearner parallely: auto_run_baselearner.py
        matlab code to run one base leaner: run_baselearner.m
        matlab code of base learner with MMCRF: baselearner.m


3. Run MAM ensemble   
 auto_run_MAMlearner.py
 run_MAMlearner.m
 mu_complete_zero.m

4. Run Majority Voting ensemble and Average Maximum marginal ensemble
 auto_run_MAV_AMMlearner.py
 run_MAV_AMMlearner.m

5. Run parameter selection with base learner
 auto_run_parameter_selection.py
 run_parameter_selection.m
